import { it, expect, describe } from "vitest";
import ContactForm from "../components/ContactForm";

describe(ContactForm, () => {
  it("Displays Correctly initial count", () => {});
});

describe("group", () => {
  it("should", () => {
    expect(1).toBeTruthy();
  });
});

